enum NavRoute {
  home,
  employees,
  attendance,
  summary,
  information,
  settings,
}

enum UserRole {
  admin,
  user,
  viewer,
}

enum ProjectStatus {
  pending,
  inProgress,
  done,
}

enum WorkspaceType {
  main,
  finance,
  custom,
}

enum CalendarViewType {
  month,
  week,
}